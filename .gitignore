# Private repo — journal content is tracked. Only OS/editor/runtime
# noise is excluded here.
.DS_Store
Thumbs.db
*.swp

# Docker Desktop / WSL runtime junk if the lab folder ends up alongside
named_dump.db
*.jnl